<?xml version="1.0" encoding="utf-8"?><?xml-stylesheet type="text/xsl" href="rss.xsl"?>
<rss version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/">
    <channel>
        <title>Rayen Dhmaied Blog</title>
        <link>https://rayen-dhmaied.github.io/portfolio/blog</link>
        <description>Rayen Dhmaied Blog</description>
        <lastBuildDate>Tue, 01 Jan 2030 00:00:00 GMT</lastBuildDate>
        <docs>https://validator.w3.org/feed/docs/rss2.html</docs>
        <generator>https://github.com/jpmonette/feed</generator>
        <language>en</language>
        <item>
            <title><![CDATA[Welcome to my blog]]></title>
            <link>https://rayen-dhmaied.github.io/portfolio/blog/welcome</link>
            <guid>https://rayen-dhmaied.github.io/portfolio/blog/welcome</guid>
            <pubDate>Tue, 01 Jan 2030 00:00:00 GMT</pubDate>
            <description><![CDATA[Hi there.]]></description>
            <content:encoded><![CDATA[<p>Hi there.</p>
<p>This space is where I share work in more detail. It includes projects I’ve worked on, practical notes, and observations from things I’ve built or learned along the way.</p>
<p>The focus is on showing how things were done, the decisions behind them, and what worked or didn’t. It's the kind of context that doesn’t fit in a resume or quick summary.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-youll-find-here">What You’ll Find Here<a href="https://rayen-dhmaied.github.io/portfolio/blog/welcome#what-youll-find-here" class="hash-link" aria-label="Direct link to What You’ll Find Here" title="Direct link to What You’ll Find Here" translate="no">​</a></h3>
<ul>
<li class="">🛠 Project overviews and technical decisions</li>
<li class="">📈 Results and outcomes</li>
<li class="">🧩 Problem-solving approaches</li>
<li class="">✍️ Notes on tools, ideas, and lessons learned</li>
</ul>
<p>The goal is to keep a record of my work while also creating a resource others might find useful. Whether you’re exploring similar problems or just curious about the process, there’s something here for you.</p>
<p>Thanks for reading.</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Splitting a Python Dockerfile into Build and Runtime Stages]]></title>
            <link>https://rayen-dhmaied.github.io/portfolio/blog/python-docker-multi-stage</link>
            <guid>https://rayen-dhmaied.github.io/portfolio/blog/python-docker-multi-stage</guid>
            <pubDate>Fri, 05 Dec 2025 00:00:00 GMT</pubDate>
            <description><![CDATA[I shipped a Flask backend for a quiz platform. The first time CI tried to push the production image to GitHub Container Registry, it died midway:]]></description>
            <content:encoded><![CDATA[<p>I shipped a Flask backend for a quiz platform. The first time CI tried to push the production image to GitHub Container Registry, it died midway:</p>
<div class="language-text codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-text codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#393A34"><span class="token plain">#15 pushing layer dc355e6e88a5 145.61MB / 673.11MB 9.0s done</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">#15 ERROR: unknown blob</span><br></span></code></pre></div></div>
<p>The image was around 700 MB. One layer on its own was 673 MB. Every Pod cold-start was pulling that off the registry, and pushes kept timing out because the single big layer took too long to upload in one shot.</p>
<p>The Dockerfile was short and looked fine until you stared at it:</p>
<div class="language-dockerfile codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-dockerfile codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#393A34"><span class="token plain">FROM python:3.12-alpine</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">RUN apk add --no-cache \</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    gcc \</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    musl-dev \</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    libffi-dev \</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    openssl-dev \</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    postgresql-dev</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">WORKDIR /app</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">COPY requirements.txt .</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">RUN pip install --no-cache-dir -r requirements.txt</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">COPY . .</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">RUN adduser -D quizship &amp;&amp; chown -R quizship /app</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">USER quizship</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">CMD ["gunicorn", "app:app", "--config", "gunicorn.conf.py"]</span><br></span></code></pre></div></div>
<hr>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="why-the-image-is-so-big">Why the Image Is So Big<a href="https://rayen-dhmaied.github.io/portfolio/blog/python-docker-multi-stage#why-the-image-is-so-big" class="hash-link" aria-label="Direct link to Why the Image Is So Big" title="Direct link to Why the Image Is So Big" translate="no">​</a></h2>
<p>A Docker image is a stack of read-only layers. Once a file lands in a layer it's there for good. You can <code>rm</code> it in a later layer and the merged filesystem will hide it, but the bytes are still sitting in the previous layer and they still get pushed.</p>
<p>The line that does most of the damage is <code>apk add gcc musl-dev libffi-dev openssl-dev postgresql-dev</code>. That's a build toolchain. I needed it in the next step so <code>pip install</code> could compile the C bits of <code>cryptography</code>, <code>psycopg2</code>, and <code>bcrypt</code>. After that, gcc never runs again. But because everything happened in the same image, gcc is still sitting in the runtime image, doing nothing.</p>
<p>In my case, that toolchain plus its <code>-dev</code> headers added around 650 MB. The Python interpreter, the app code, and the installed wheels came in under 100 MB combined. The compiler was bigger than the thing it was there to compile.</p>
<p>The way out: do the build in one image and the install in another, then copy only what the runtime needs.</p>
<hr>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="two-stages-one-image">Two Stages, One Image<a href="https://rayen-dhmaied.github.io/portfolio/blog/python-docker-multi-stage#two-stages-one-image" class="hash-link" aria-label="Direct link to Two Stages, One Image" title="Direct link to Two Stages, One Image" translate="no">​</a></h2>
<p>A Dockerfile can have more than one <code>FROM</code>. Each one starts a brand new stage with its own layer history, and only the last stage actually becomes the image you push. You can name a stage with <code>AS something</code> and then pull specific files out of it from a later stage.</p>
<div class="language-dockerfile codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-dockerfile codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#393A34"><span class="token plain">FROM python:3.12-alpine AS builder</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain"># install toolchain, compile things</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">FROM python:3.12-alpine</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">COPY --from=builder /wheels /wheels</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain"># only this stage's layers end up in the published image</span><br></span></code></pre></div></div>
<p>Only the last <code>FROM</code> becomes the published image. Everything in the <code>builder</code> stage gets thrown away once the build is done. The toolchain, the build directory, whatever source tarballs <code>pip</code> pulled down: none of it ships.</p>
<p>For Python the split is straightforward: compile in one stage, install in the other. The build stage turns every dependency into a wheel. The runtime stage installs those wheels without ever needing a compiler.</p>
<hr>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-a-wheel-is">What a Wheel Is<a href="https://rayen-dhmaied.github.io/portfolio/blog/python-docker-multi-stage#what-a-wheel-is" class="hash-link" aria-label="Direct link to What a Wheel Is" title="Direct link to What a Wheel Is" translate="no">​</a></h2>
<p>A wheel is a <code>.whl</code> file. Rename it to <code>.zip</code> and you can unzip it. Inside are the package's Python files, and for packages with C parts, already-compiled <code>.so</code> binaries for one specific Python + OS + CPU combination.</p>
<p>When you run <code>pip install psycopg2</code>, pip picks one of two paths:</p>
<ol>
<li class="">Grab a matching wheel and unzip it into <code>site-packages</code>. No compiler involved.</li>
<li class="">Grab an sdist (the source tarball), find a compiler, build the C code, install. Slower, and it needs gcc plus the dev headers.</li>
</ol>
<p>If PyPI has a wheel that matches your Python, OS, and architecture, pip uses it. If not (musl-based Alpine hits this sometimes because most projects publish glibc wheels only), pip falls back to the sdist and starts compiling.</p>
<p>Wheel filenames encode the compatibility info:</p>
<div class="language-text codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-text codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#393A34"><span class="token plain">psycopg2_binary-2.9.11-cp312-cp312-musllinux_1_2_x86_64.whl</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">└─────────────┘ └─────┘ └───┘ └───┘ └──────────────────────┘</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">   package      version  py   abi   platform</span><br></span></code></pre></div></div>
<p>Pip walks the available files and picks the one whose tags match the host it's running on.</p>
<p>The two-stage Dockerfile uses two pip commands:</p>
<ol>
<li class=""><code>pip wheel -r requirements.txt --wheel-dir /wheels</code>. Runs in the builder, where gcc is available. Produces a wheel for every package, including the ones that had to be compiled.</li>
<li class=""><code>pip install --no-index --find-links=/wheels -r requirements.txt</code>. Runs in the runtime, where gcc is not available. <code>--no-index</code> keeps pip away from PyPI so it only uses the wheels we just built.</li>
</ol>
<p>The compiler and dev headers stay in the builder stage. Only the wheels (plus the small <code>.so</code> libraries they need at runtime) cross over to the runtime stage.</p>
<hr>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-new-dockerfile">The New Dockerfile<a href="https://rayen-dhmaied.github.io/portfolio/blog/python-docker-multi-stage#the-new-dockerfile" class="hash-link" aria-label="Direct link to The New Dockerfile" title="Direct link to The New Dockerfile" translate="no">​</a></h2>
<div class="language-dockerfile codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-dockerfile codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#393A34"><span class="token plain"># Build stage: compile wheels with the full toolchain.</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">FROM python:3.12-alpine AS builder</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">RUN apk add --no-cache \</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    gcc \</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    musl-dev \</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    libffi-dev \</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    openssl-dev \</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    postgresql-dev</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">WORKDIR /build</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">COPY requirements.txt .</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">RUN pip wheel --no-cache-dir --wheel-dir /wheels -r requirements.txt</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain"># Runtime stage: only the wheels + the .so libraries they link against.</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">FROM python:3.12-alpine</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">RUN apk add --no-cache \</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    libpq \</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    libffi \</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    openssl</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">WORKDIR /app</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">COPY --from=builder /wheels /wheels</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">COPY requirements.txt .</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">RUN pip install --no-cache-dir --no-index --find-links=/wheels -r requirements.txt \</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    &amp;&amp; rm -rf /wheels</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">COPY . .</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">RUN adduser -D quizship &amp;&amp; chown -R quizship /app</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">USER quizship</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">ENV FLASK_APP=app:app \</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    PYTHONDONTWRITEBYTECODE=1 \</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    PYTHONUNBUFFERED=1</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">EXPOSE 5000</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">CMD ["gunicorn", "app:app", "--config", "gunicorn.conf.py"]</span><br></span></code></pre></div></div>
<p>Two things worth pointing out.</p>
<p>The builder uses <code>gcc musl-dev libffi-dev openssl-dev postgresql-dev</code>. The runtime uses <code>libpq libffi openssl</code>. Same packages with the <code>-dev</code> dropped. The <code>-dev</code> versions carry headers and <code>.a</code> archives that gcc links against; the plain versions are the shared libraries that the compiled code actually calls at runtime. Debian uses the same pattern, just with different names: <code>libpq-dev</code> to build, <code>libpq5</code> to run.</p>
<p>The runtime stage installs with <code>--no-index --find-links=/wheels</code>, so pip never talks to PyPI. After the install I <code>rm -rf /wheels</code> because the staging copy isn't doing anything useful sitting on disk. The final layer ends up being the installed Python packages and nothing else.</p>
<hr>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-i-got-wrong-on-the-first-try">What I Got Wrong on the First Try<a href="https://rayen-dhmaied.github.io/portfolio/blog/python-docker-multi-stage#what-i-got-wrong-on-the-first-try" class="hash-link" aria-label="Direct link to What I Got Wrong on the First Try" title="Direct link to What I Got Wrong on the First Try" translate="no">​</a></h2>
<p>I forgot to put <code>libpq</code> in the runtime stage. Build went green, container started, first DB call blew up:</p>
<div class="language-text codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-text codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#393A34"><span class="token plain">ImportError: libpq.so.5: cannot open shared object file: No such file or directory</span><br></span></code></pre></div></div>
<p>Wheels with C extensions don't bundle the C library they call into. They ship the Python binding and <code>.so</code> glue, and at runtime that glue does a normal dynamic-library lookup. <code>psycopg2</code> looks for libpq, <code>cryptography</code> looks for libssl, and so on. The builder had all of those (they came with the <code>-dev</code> packages, which is why <code>pip wheel</code> worked). The runtime stage had none of them.</p>
<p>Same fix every time: for every <code>*-dev</code> package in the builder, drop the suffix and <code>apk add</code> the runtime version. Sometimes the runtime package has a different name than you'd guess (Debian uses <code>libfoo5</code>, Alpine usually just keeps <code>libfoo</code>), so when something fails to import after a build, check the Alpine package index first before going looking for anything more exotic.</p>
<hr>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="layer-cache-order-matters">Layer Cache Order Matters<a href="https://rayen-dhmaied.github.io/portfolio/blog/python-docker-multi-stage#layer-cache-order-matters" class="hash-link" aria-label="Direct link to Layer Cache Order Matters" title="Direct link to Layer Cache Order Matters" translate="no">​</a></h2>
<p>There's a reason <code>COPY requirements.txt .</code> shows up before <code>COPY . .</code>. Docker invalidates a layer's cache when its inputs change, and "inputs" includes anything copied in. If you <code>COPY . .</code> before installing dependencies, every code edit busts the wheel cache and CI recompiles <code>cryptography</code> from scratch on every push.</p>
<p>With the order I went with:</p>
<ol>
<li class="">Only <code>requirements.txt</code> gets copied. Cache key is small and rarely changes.</li>
<li class=""><code>pip wheel</code> runs against it. Cached unless <code>requirements.txt</code> itself changed.</li>
<li class=""><code>pip install</code> runs against the wheels. Cached if <code>/wheels</code> didn't change.</li>
<li class="">Finally <code>COPY . .</code> brings in the source. Change this all you want; the wheel layers above stay warm.</li>
</ol>
<p>Touch the app code and you only rebuild from step 4. Touch <code>requirements.txt</code> and you rebuild from step 2. The slow steps stay cached until their inputs actually change.</p>
<hr>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="how-it-played-out">How It Played Out<a href="https://rayen-dhmaied.github.io/portfolio/blog/python-docker-multi-stage#how-it-played-out" class="hash-link" aria-label="Direct link to How It Played Out" title="Direct link to How It Played Out" translate="no">​</a></h2>
<p>The image dropped from around 700 MB to around 150 MB. The biggest layer in the runtime image is now ~110 MB, mostly the Python interpreter plus the installed wheels. Before, that one layer was 673 MB on its own.</p>
<p>The GHCR push that failed on <code>unknown blob</code> went through on the next pipeline run. Pod cold-starts on fresh nodes used to take 30+ seconds; now they finish in a few.</p>
<p>CI took about as long as before. We still compile every wheel that needs compiling. The difference is that the compiler stays in the builder stage and never reaches the final image.</p>
<hr>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="sniff-tests-for-other-projects">Sniff Tests for Other Projects<a href="https://rayen-dhmaied.github.io/portfolio/blog/python-docker-multi-stage#sniff-tests-for-other-projects" class="hash-link" aria-label="Direct link to Sniff Tests for Other Projects" title="Direct link to Sniff Tests for Other Projects" translate="no">​</a></h2>
<p>A few signs an image needs this treatment:</p>
<ul>
<li class=""><code>python:3.12-alpine</code> is around 50 MB. If your image is over 200 MB, something else got in.</li>
<li class="">You installed anything with <code>-dev</code>, <code>-devel</code>, <code>gcc</code>, <code>g++</code>, <code>make</code>, <code>cargo</code>, <code>go</code>, or <code>node-gyp</code>.</li>
<li class="">A registry push stalls or fails on one fat layer.</li>
<li class="">Pods take a long time to start on nodes that have not pulled the image before.</li>
</ul>
<p>The same split works for Go and Rust, and the gap is even bigger there because their build toolchains weigh more than Alpine + gcc. Build the binary in stage one, copy it into a <code>scratch</code> or <code>distroless</code> base in stage two.</p>
<p>The first deploy gives you a smaller image. After that you get faster pulls when a Pod lands on a cold node, faster CI on cached layers, and less surface area in production if a container ever gets compromised.</p>]]></content:encoded>
            <category>docker</category>
            <category>python</category>
            <category>ci</category>
            <category>kubernetes</category>
        </item>
        <item>
            <title><![CDATA[Refactoring Terraform into Modules Safely]]></title>
            <link>https://rayen-dhmaied.github.io/portfolio/blog/refactoring-terraform-into-modules</link>
            <guid>https://rayen-dhmaied.github.io/portfolio/blog/refactoring-terraform-into-modules</guid>
            <pubDate>Thu, 14 Aug 2025 00:00:00 GMT</pubDate>
            <description><![CDATA[I worked on an older Terraform project that managed a full EKS stack: VPC, cluster, node groups, storage, and around ten Kubernetes add-ons. The whole stack lived in one main.tf file with more than a thousand lines.]]></description>
            <content:encoded><![CDATA[<p>I worked on an older Terraform project that managed a full EKS stack: VPC, cluster, node groups, storage, and around ten Kubernetes add-ons. The whole stack lived in one <code>main.tf</code> file with more than a thousand lines.</p>
<p>It worked. It was also hard to change. Adding one add-on meant scrolling through networking, IAM, EKS, and Helm resources just to find the right place.</p>
<div class="language-bash codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-bash codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#393A34"><span class="token plain">.</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">├── main.tf      # 1000+ lines: VPC, EKS, add-ons, storage</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">├── variables.tf</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">├── outputs.tf</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">├── providers.tf</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">├── backend.tf</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">└── vars.tfvars</span><br></span></code></pre></div></div>
<p>I wanted modules, but I did not want Terraform to recreate a live VPC with an EKS cluster attached to it. The refactor had to move code and state together.</p>
<hr>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-risk">The Risk<a href="https://rayen-dhmaied.github.io/portfolio/blog/refactoring-terraform-into-modules#the-risk" class="hash-link" aria-label="Direct link to The Risk" title="Direct link to The Risk" translate="no">​</a></h2>
<p>Terraform tracks resources by state address.</p>
<div class="language-hcl codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-hcl codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#393A34"><span class="token plain"># before</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">aws_vpc.main</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain"># after</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">module.vpc.aws_vpc.main</span><br></span></code></pre></div></div>
<p>Those two addresses point to the same real VPC, but Terraform does not know that unless you tell it. If you move the resource block into a module and run <code>terraform apply</code>, Terraform can plan a destroy/create because the old address disappeared and a new address appeared.</p>
<p>For an EKS foundation, that plan tears down the base layer.</p>
<p>Use <code>terraform state mv</code> to rename the state address without touching the real infrastructure:</p>
<ol>
<li class="">Copy resources into a module.</li>
<li class="">Add the module call in the root.</li>
<li class="">Move each old state address to its new module address.</li>
<li class="">Run <code>terraform plan</code>.</li>
<li class="">Continue only when the plan says no changes.</li>
</ol>
<hr>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="pick-module-boundaries">Pick Module Boundaries<a href="https://rayen-dhmaied.github.io/portfolio/blog/refactoring-terraform-into-modules#pick-module-boundaries" class="hash-link" aria-label="Direct link to Pick Module Boundaries" title="Direct link to Pick Module Boundaries" translate="no">​</a></h2>
<p>Start with lifecycle and dependencies, not folder names.</p>
<p><strong>Group one concern.</strong> VPC resources belong together: <code>aws_vpc</code>, subnets, route tables, internet gateway, NAT gateway, and EIPs.</p>
<p><strong>Keep shared lifecycle together.</strong> NAT gateways and their EIPs usually move as one unit.</p>
<p><strong>Cut dependencies in one direction.</strong> Sketch the module flow:</p>
<div class="language-hcl codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-hcl codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#393A34"><span class="token plain">module "vpc" -&gt; module "eks" -&gt; module "addons"</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">#  vpc_id        subnet_ids      cluster_name</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">#  subnet_ids    oidc_arn        oidc_arn</span><br></span></code></pre></div></div>
<p>Each arrow should map to an output on the left and an input on the right. If two groups need values from each other, they probably belong in the same module.</p>
<p>For this stack, I split the code into:</p>
<ul>
<li class=""><code>vpc</code>: networking resources</li>
<li class=""><code>eks</code>: cluster, node groups, OIDC, cluster IAM</li>
<li class=""><code>addons</code>: controllers, monitoring add-ons, Kubernetes manifests</li>
<li class=""><code>storage</code>: storage classes, EBS/EFS-related resources</li>
</ul>
<hr>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="audit-state-first">Audit State First<a href="https://rayen-dhmaied.github.io/portfolio/blog/refactoring-terraform-into-modules#audit-state-first" class="hash-link" aria-label="Direct link to Audit State First" title="Direct link to Audit State First" translate="no">​</a></h2>
<p>Before writing modules, list the current state:</p>
<div class="language-bash codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-bash codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#393A34"><span class="token plain">terraform state list</span><br></span></code></pre></div></div>
<p>Every managed resource needs a destination address. Build a mapping table before you run any <code>state mv</code>.</p>
<p>For the VPC module, the mapping looked like this:</p>
<table><thead><tr><th>Old</th><th>New</th></tr></thead><tbody><tr><td><code>aws_vpc.main</code></td><td><code>module.vpc.aws_vpc.main</code></td></tr><tr><td><code>aws_internet_gateway.igw[0]</code></td><td><code>module.vpc.aws_internet_gateway.igw[0]</code></td></tr><tr><td><code>aws_eip.eip[0]</code></td><td><code>module.vpc.aws_eip.eip[0]</code></td></tr><tr><td><code>aws_nat_gateway.nat[0]</code></td><td><code>module.vpc.aws_nat_gateway.nat[0]</code></td></tr><tr><td><code>aws_subnet.private[0]</code></td><td><code>module.vpc.aws_subnet.private[0]</code></td></tr><tr><td><code>aws_subnet.private[1]</code></td><td><code>module.vpc.aws_subnet.private[1]</code></td></tr><tr><td><code>aws_subnet.private[2]</code></td><td><code>module.vpc.aws_subnet.private[2]</code></td></tr><tr><td><code>aws_subnet.public[0]</code></td><td><code>module.vpc.aws_subnet.public[0]</code></td></tr><tr><td><code>aws_subnet.public[1]</code></td><td><code>module.vpc.aws_subnet.public[1]</code></td></tr><tr><td><code>aws_subnet.public[2]</code></td><td><code>module.vpc.aws_subnet.public[2]</code></td></tr></tbody></table>
<p>Data sources (<code>data.*</code>) do not need state moves. Terraform reads them during planning. If several modules need the same data source, keep it in the root and pass values down as variables. If only one module uses it, put it inside that module.</p>
<hr>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="write-one-module">Write One Module<a href="https://rayen-dhmaied.github.io/portfolio/blog/refactoring-terraform-into-modules#write-one-module" class="hash-link" aria-label="Direct link to Write One Module" title="Direct link to Write One Module" translate="no">​</a></h2>
<p>Move one module at a time. Do not move VPC, EKS, add-ons, and storage in one commit.</p>
<p>Each module gets the same three files:</p>
<div class="language-bash codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-bash codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#393A34"><span class="token plain">modules/vpc/</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">├── main.tf</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">├── variables.tf</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">└── outputs.tf</span><br></span></code></pre></div></div>
<p>Copy the resource blocks exactly. Do not rename resources, change tags, clean up variables, or improve naming during the move. Save those changes for a later commit after the state move produces a clean plan.</p>
<p>Example VPC module:</p>
<div class="language-hcl codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-hcl codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#393A34"><span class="token plain"># modules/vpc/variables.tf</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">variable "region"   { type = string }</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">variable "env"      { type = string }</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">variable "eks_name" { type = string }</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">variable "vpc_cidr_block"       { type = string }</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">variable "private_subnets"      { type = list(string) }</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">variable "public_subnets"       { type = list(string) }</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">variable "enable_nat_gateway"   { type = bool; default = true }</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">variable "enable_dns_support"   { type = bool; default = true }</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">variable "enable_dns_hostnames" { type = bool; default = true }</span><br></span></code></pre></div></div>
<div class="language-hcl codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-hcl codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#393A34"><span class="token plain"># modules/vpc/main.tf</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">resource "aws_vpc" "main" {</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  cidr_block           = var.vpc_cidr_block</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  enable_dns_support   = var.enable_dns_support</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  enable_dns_hostnames = var.enable_dns_hostnames</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  tags = {</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    Name = "${var.region}-${var.env}-${var.eks_name}-vpc"</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  }</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">}</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">resource "aws_internet_gateway" "igw" {</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  count  = 1</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  vpc_id = aws_vpc.main.id</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  tags = { Name = "${var.env}-${var.eks_name}-igw" }</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">}</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">resource "aws_eip" "eip" {</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  count  = var.enable_nat_gateway ? 1 : 0</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  domain = "vpc"</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">}</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">resource "aws_nat_gateway" "nat" {</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  count         = var.enable_nat_gateway ? 1 : 0</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  allocation_id = aws_eip.eip[0].id</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  subnet_id     = aws_subnet.public[0].id</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  depends_on = [aws_internet_gateway.igw[0]]</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">}</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">resource "aws_subnet" "private" {</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  count             = length(var.private_subnets)</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  vpc_id            = aws_vpc.main.id</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  cidr_block        = var.private_subnets[count.index]</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  availability_zone = "${var.region}${["a", "b", "c"][count.index]}"</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  tags = { Name = "${var.env}-${var.eks_name}-private-${count.index}" }</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">}</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">resource "aws_subnet" "public" {</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  count             = length(var.public_subnets)</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  vpc_id            = aws_vpc.main.id</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  cidr_block        = var.public_subnets[count.index]</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  availability_zone = "${var.region}${["a", "b", "c"][count.index]}"</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  tags = { Name = "${var.env}-${var.eks_name}-public-${count.index}" }</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">}</span><br></span></code></pre></div></div>
<div class="language-hcl codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-hcl codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#393A34"><span class="token plain"># modules/vpc/outputs.tf</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">output "vpc_id" {</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  value = aws_vpc.main.id</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">}</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">output "private_subnet_ids" {</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  value = aws_subnet.private[*].id</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">}</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">output "all_subnet_ids" {</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  value = concat(</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    aws_subnet.private[*].id,</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    aws_subnet.public[*].id</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  )</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">}</span><br></span></code></pre></div></div>
<hr>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="declare-non-hashicorp-providers">Declare Non-HashiCorp Providers<a href="https://rayen-dhmaied.github.io/portfolio/blog/refactoring-terraform-into-modules#declare-non-hashicorp-providers" class="hash-link" aria-label="Direct link to Declare Non-HashiCorp Providers" title="Direct link to Declare Non-HashiCorp Providers" translate="no">​</a></h2>
<p>Terraform assumes providers come from the <code>hashicorp</code> namespace unless you say otherwise. If a module uses a provider like <code>gavinbunney/kubectl</code>, declare it inside the module.</p>
<div class="language-hcl codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-hcl codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#393A34"><span class="token plain"># modules/addons/providers.tf</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">terraform {</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  required_providers {</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    kubectl = {</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">      source  = "gavinbunney/kubectl"</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">      version = "~&gt; 1.19.0"</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    }</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  }</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">}</span><br></span></code></pre></div></div>
<p>Do not configure the provider inside the module. Keep region, credentials, cluster endpoint, and token settings in the root. The module declares what it needs, and the root passes the configured provider in.</p>
<div class="language-hcl codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-hcl codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#393A34"><span class="token plain">module "addons" {</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  source = "./modules/addons"</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  providers = {</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    kubectl = kubectl</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  }</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">}</span><br></span></code></pre></div></div>
<hr>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="update-the-root-module">Update the Root Module<a href="https://rayen-dhmaied.github.io/portfolio/blog/refactoring-terraform-into-modules#update-the-root-module" class="hash-link" aria-label="Direct link to Update the Root Module" title="Direct link to Update the Root Module" translate="no">​</a></h2>
<p>Replace the moved resource blocks with a module call. Then update references to use module outputs.</p>
<div class="language-hcl codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-hcl codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#393A34"><span class="token plain"># main.tf</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">module "vpc" {</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  source = "./modules/vpc"</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  region             = var.region</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  env                = var.env</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  eks_name           = var.eks_name</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  vpc_cidr_block     = var.vpc_cidr_block</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  private_subnets    = var.private_subnets</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  public_subnets     = var.public_subnets</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  enable_nat_gateway = var.enable_nat_gateway</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">}</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">module "eks" {</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  source = "./modules/eks"</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  vpc_id     = module.vpc.vpc_id</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  subnet_ids = module.vpc.all_subnet_ids</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">}</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">module "addons" {</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  source = "./modules/addons"</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  providers = {</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    kubectl = kubectl</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  }</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  cluster_name      = module.eks.cluster_name</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  oidc_provider_arn = module.eks.oidc_provider_arn</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">}</span><br></span></code></pre></div></div>
<p>Run <code>terraform init</code> after adding module sources.</p>
<div class="language-bash codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-bash codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#393A34"><span class="token plain">terraform init</span><br></span></code></pre></div></div>
<hr>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="move-the-state">Move the State<a href="https://rayen-dhmaied.github.io/portfolio/blog/refactoring-terraform-into-modules#move-the-state" class="hash-link" aria-label="Direct link to Move the State" title="Direct link to Move the State" translate="no">​</a></h2>
<p>Move every old address to its new module address.</p>
<div class="language-bash codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-bash codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#393A34"><span class="token plain">terraform state mv \</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  aws_vpc.main \</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  module.vpc.aws_vpc.main</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">terraform state mv \</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  'aws_subnet.private[0]' \</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  'module.vpc.aws_subnet.private[0]'</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">terraform state mv \</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  'aws_nat_gateway.nat[0]' \</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  'module.vpc.aws_nat_gateway.nat[0]'</span><br></span></code></pre></div></div>
<p>Keep indexes and keys exactly as Terraform lists them.</p>
<div class="language-hcl codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-hcl codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#393A34"><span class="token plain">aws_subnet.private[0]</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">module.vpc.aws_subnet.private[0]</span><br></span></code></pre></div></div>
<p>For <code>for_each</code> resources with string keys, quote the full address:</p>
<div class="language-bash codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-bash codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#393A34"><span class="token plain">terraform state mv \</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  'aws_iam_role_policy_attachment.this["AmazonEKSWorkerNodePolicy"]' \</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  'module.eks.aws_iam_role_policy_attachment.this["AmazonEKSWorkerNodePolicy"]'</span><br></span></code></pre></div></div>
<p>For repeated resources, script the move. Add <code>set -e</code> so the script stops on the first failed move.</p>
<div class="language-bash codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-bash codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#393A34"><span class="token plain">#!/bin/bash</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">set -e</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">for key in $(terraform state list | grep 'aws_subnet.private'); do</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  suffix=$(echo "$key" | sed 's/aws_subnet.private//')</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">  terraform state mv \</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    "aws_subnet.private${suffix}" \</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    "module.vpc.aws_subnet.private${suffix}"</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">done</span><br></span></code></pre></div></div>
<p>Run the script for one module, then plan before touching the next module.</p>
<hr>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="expect-a-no-change-plan">Expect a No-Change Plan<a href="https://rayen-dhmaied.github.io/portfolio/blog/refactoring-terraform-into-modules#expect-a-no-change-plan" class="hash-link" aria-label="Direct link to Expect a No-Change Plan" title="Direct link to Expect a No-Change Plan" translate="no">​</a></h2>
<p>After the state moves, run:</p>
<div class="language-bash codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-bash codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#393A34"><span class="token plain">terraform plan</span><br></span></code></pre></div></div>
<p>You want this output:</p>
<div class="language-text codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-text codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#393A34"><span class="token plain">No changes. Your infrastructure matches the configuration.</span><br></span></code></pre></div></div>
<p>If Terraform shows changes, stop. Do not apply. Find the resource with a diff and trace the changed input back to the module call or output. The common causes are:</p>
<ul>
<li class="">A tag changed during the copy.</li>
<li class="">A default value differs between root and module variables.</li>
<li class="">A list order changed.</li>
<li class="">A reference now points to a different output.</li>
<li class="">A provider alias did not get passed into the module.</li>
</ul>
<p>Once the plan is clean, commit the module move. Then repeat the same process for the next module.</p>
<p>The final structure looked like this:</p>
<div class="language-bash codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-bash codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#393A34"><span class="token plain">.</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">├── main.tf          # module calls only</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">├── variables.tf</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">├── outputs.tf</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">├── providers.tf</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">├── backend.tf</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">└── modules/</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    ├── vpc/</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    │   ├── main.tf</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    │   ├── variables.tf</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    │   └── outputs.tf</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    ├── eks/</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    │   ├── main.tf</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    │   ├── variables.tf</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    │   └── outputs.tf</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">    └── addons/</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">        ├── main.tf</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">        ├── variables.tf</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">        ├── outputs.tf</span><br></span><span class="token-line" style="color:#393A34"><span class="token plain">        └── providers.tf</span><br></span></code></pre></div></div>
<p>The folder layout helped, but the safety came from the process: move code and state together, one module at a time, and require a no-change plan before moving on.</p>]]></content:encoded>
            <category>terraform</category>
            <category>iac</category>
            <category>aws</category>
        </item>
    </channel>
</rss>